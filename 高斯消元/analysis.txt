通过化三角方式消元法计算方程解的时间复杂度为O(n^3)。消元法可以分为消元过程和回代求解过程。计算机在计算时有一定的舍入误差，在顺序消元法中，没有选择主元仅依次进行化三角操作，若a[j][i]>a[i][i],则作除法后会导致舍入误差的放大，使最后的结果不够可靠；而使用列主元消去法，通过选择每一列中最大的元素作为主元，调换方程的次序，使得每一次确定a[j][i]/a[i][i]的值总是小于1从而避免误差的放大，因此列主元消元法更加稳定。
